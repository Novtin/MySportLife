<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/list"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/list"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyDish"
        tools:targetApi="31">
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <activity android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChosenNewActivity"/>
        <activity android:name=".ChosenRecipeActivity"/>
        <activity android:name=".EditNoteActivity"/>
        <activity android:name=".FavoriteNewsActivity"/>
        <activity android:name=".FavoriteRecipesActivity"/>
        <activity android:name=".SaveNoteActivity"/>
        <activity android:name=".NotesActivity"/>
        <activity android:name=".MapActivity"/>
        <activity android:name=".SportActivity"/>
        <activity android:name=".NewsActivity"/>

    </application>

</manifest>